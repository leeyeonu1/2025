import streamlit as st

# MBTI별 정보와 공부법 데이터를 딕셔너리 형태로 저장 (이 부분은 연우님이 직접 채워주셔야 해요!)
mbti_data = {
    'ISTJ': {
        '성격': '현실적이고 책임감이 강해요.',
        '공부법': ['체계적인 계획 수립 후 실행', '교과서 중심의 반복 학습', '정확한 개념 이해에 집중']
    },
    'INFJ': {
        '성격': '이상적이고 통찰력이 뛰어나요.',
        '공부법': ['의미와 맥락을 파악하며 학습', '자신만의 방식으로 정리', '토론이나 발표를 통해 심화']
    },
    # ... 다른 MBTI 유형도 추가
}

st.set_page_config(
    page_title="MBTI 맞춤 공부법 추천",
    page_icon="📚",
    layout="centered" # wide 로 하면 화면이 넓게 사용돼요
)

st.title('✨ MBTI 맞춤 공부법 추천 사이트 ✨')
st.write('나의 MBTI 유형에 딱 맞는 공부법을 찾아보세요!')

# 사이드바에서 MBTI 선택
st.sidebar.header('나의 MBTI 유형은?')
selected_mbti = st.sidebar.selectbox(
    '아래에서 당신의 MBTI 유형을 선택해주세요.',
    list(mbti_data.keys())
)

# 메인 콘텐츠 영역
if selected_mbti:
    st.markdown(f"## 💡 **{selected_mbti}** 유형을 위한 추천 공부법!")
    st.markdown("---") # 구분선

    # MBTI 성격 특징
    st.subheader(f'{selected_mbti}는 어떤 사람인가요?')
    st.markdown(f"> {mbti_data[selected_mbti]['성격']}")

    st.markdown("<br>", unsafe_allow_html=True) # 줄바꿈

    # 맞춤 공부법 추천
    st.subheader(f'📚 {selected_mbti}에게 딱 맞는 공부법')
    for i, method in enumerate(mbti_data[selected_mbti]['공부법']):
        st.write(f"✅ {method}")
